

## Workspace
# Initialize
git init

# Add All
git add .

# Commit Message
git commit -m "<message_nimo>"

# Only use once if first time lang naka create ug repo, exclude lang ni nga part if na execute na
git remote add origin <imo_repo_link>

# Push Changes
git push origin develop
